<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="webtaskmanager.mapper.TaskMapper">
    <insert id="insertTask" parameterType="webtaskmanager.model.Task">
        INSERT INTO task (title, description, action) VALUES (#{task.title}, #{task.description}, #{task.action});
    </insert>

    <update id="updateTask">
        UPDATE task SET title=#{task.title}, description=#{task.description}, action=#{task.action} WHERE id=#{task.id} ;
    </update>

    <delete id="deleteTask" parameterType="int">
        DELETE from task WHERE ID = #{id};
    </delete>

    <select id="findAllTasks" resultMap="result">
        SELECT * FROM task
        ORDER BY id ASC;
    </select>

    <select id="findById" parameterType="int" resultType="webtaskmanager.model.Task">
        SELECT * FROM task WHERE ID = #{id};
    </select>

    <select id="countTasks" resultType="int">
        SELECT count(*) FROM task
        WHERE title LIKE '%' #{title} '%'
        AND action LIKE '%' #{action} '%';
    </select>

    <select id="findAllByPage" resultMap="result">
        SELECT * FROM task
        WHERE title LIKE '%' #{title} '%'
        AND action LIKE '%' #{action} '%'
        ORDER BY id ASC
        LIMIT #{pageable.offset}, #{pageable.pageSize};
    </select>

    <resultMap id = "result" type = "webtaskmanager.model.Task">
        <result property = "id" column = "ID"/>
        <result property = "title" column = "TITLE"/>
        <result property = "description" column = "DESCRIPTION"/>
        <result property = "action" column = "ACTION"/>
    </resultMap>
</mapper>