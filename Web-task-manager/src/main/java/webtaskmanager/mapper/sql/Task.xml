<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Task">
    <insert id="insertTask">
        INSERT INTO task (title, description, action) VALUES (#{task.getTitle()}, #{task.getDescription()}, #{task.getAction()});
    </insert>

    <update id="updateTask">
        UPDATE task WHERE id=#{id} SET title=#{task.getTitle()}, description=#{task.getDescription()}, action=#{task.getAction()};
    </update>

    <delete id="deleteTask" parameterType="int">
        DELETE from task WHERE ID = #{id};
    </delete>

    <select id="findAllTasks" resultMap="result">
        SELECT * FROM task;
    </select>

    <select id="findById" parameterType="int" resultType="result">
        SELECT * FROM task WHERE ID = #{id};
    </select>

    <select id="countTasks" resultType="int">
        SELECT count(*) FROM task;
    </select>
    
    <select id="findAllByPage" resultType="result">
        SELECT * FROM task WHERE title LIKE '%' #{title} '%'  AND action LIKE '%' #{action} '%' LIMIT #{pageable.offset}, #{pageable.pageSize};
    </select>

    <resultMap id = "result" type = "webtaskmanager.model.Task">
        <result property = "id" column = "ID"/>
        <result property = "title" column = "TITLE"/>
        <result property = "description" column = "DESCRIPTION"/>
        <result property = "action" column = "ACTION"/>
    </resultMap>
</mapper>